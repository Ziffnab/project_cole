using System;
using System.IO;
using UnityEngine;

public static class TemplateLoader
{
    public static string LoadTemplate()
    {
        // Implement logic to read the HTML template from the private-ui directory into a string
        return "";
    }
}